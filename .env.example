# --- X (Twitter) credentials ---
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# --- LLM (OpenAI or OpenRouter-compatible) ---
OPENAI_API_KEY=
# Optional: point to OpenRouter or self-hosted endpoint
# OPENAI_BASE_URL=
# OPENAI_MODEL=gpt-4o-mini

# --- Posting schedule (minutes) ---
POST_INTERVAL_MIN=120
POST_INTERVAL_MAX=240
POST_IMMEDIATELY=false

# --- Safety & ops ---
MAX_TWEET_LENGTH=280
DRY_RUN=true

# --- (Optional) Active hours in Europe/Athens ---
ACTIVE_HOURS_START=08:00
ACTIVE_HOURS_END=22:00
TIMEZONE=Europe/Athens
